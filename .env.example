# Optional defaults loaded by the source checkout's ./mill launcher.
# The installed Python entrypoint reads the process environment instead.
# Choose credentials for the backend selected with --agent; never commit values.
CODEX_API_KEY=
ANTHROPIC_API_KEY=
CLAUDE_CODE_OAUTH_TOKEN=

# Optional source repository and image defaults.
# REPO_PATH=/absolute/path/to/repo
# AGENTMILL_IMAGE=agentmill:latest
# CHECK_CMD=uv run pytest -q

# Set task, model, setup, checks, and finite limits through CLI flags or --spec.
