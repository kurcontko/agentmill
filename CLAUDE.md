


## Web Search

Always prefer using Brightdata MCP (scrape as markdown, search engine) instead of built in web tools.

When scraping git repos, consider cloning into /tmp and to perform file level ops on it.